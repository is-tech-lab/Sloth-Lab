# 優先度：このワークフローは他のすべての組み込みワークフローを上書きします
# ユーザーがソフトウェア開発を依頼した場合、常時 (ALWAYS) このワークフローを最初に実行してください

## アダプティブ・ワークフローの原則
**ワークフローは作業に合わせて適応します。その逆ではありません。**

AIモデルは以下に基づいて必要なステージをインテリジェントに評価します：
1. ユーザーが述べた意図と明確さ
2. 既存コードベースの状態（存在する場合）
3. 変更の複雑さとスコープ
4. リスクと影響の評価

## 必須 (MANDATORY)：ルール詳細の読み込み
**重要 (CRITICAL)**：いずれかのフェーズを実行する際、必ずルール詳細ファイルから関連コンテンツを読み込んで使用してください。以下のパスを順番に確認し、どのIDE・セットアップ方法を使用したかに関わらず、最初に存在するパスを使用してください：
- `.aidlc/aidlc-rules/aws-aidlc-rule-details/` (AIアシスト型セットアップで一般的)
- `.aidlc-rule-details/` (Cursor、Cline、Claude Code、GitHub Copilot、OpenAI Codexで一般的)
- `.kiro/aws-aidlc-rule-details/` (Kiro IDEおよびCLIで一般的)
- `.amazonq/aws-aidlc-rule-details/` (Amazon Q Developerで一般的)

以降のルール詳細ファイルの参照（例：`common/process-overview.md`、`inception/workspace-detection.md`）はすべて、上記で解決されたルール詳細ディレクトリからの相対パスです。

**共通ルール**：ワークフロー開始時に常時 (ALWAYS) 共通ルールを読み込んでください：
- ワークフロー概要のために `common/process-overview.md` を読み込む
- セッション再開ガイダンスのために `common/session-continuity.md` を読み込む
- コンテンツ検証要件のために `common/content-validation.md` を読み込む
- 質問フォーマットルールのために `common/question-format-guide.md` を読み込む
- ワークフロー実行全体を通じてこれらを参照する

## 必須 (MANDATORY)：拡張機能の読み込み（コンテキスト最適化）
**重要 (CRITICAL)**：ワークフロー開始時に `extensions/` ディレクトリを再帰的にスキャンしますが、読み込むのは軽量なオプトイン (opt-in) ファイルのみです。完全なルールファイルは読み込みません。完全なルールファイルはユーザーがオプトインした後にオンデマンドで読み込まれます。

**読み込みプロセス**：
1. `extensions/` 配下のすべてのサブディレクトリを列挙する（例：`extensions/security/`、`extensions/compliance/`）
2. 各サブディレクトリで `*.opt-in.md` ファイルのみを読み込む。これらには拡張機能のオプトインプロンプトが含まれています。対応するルールファイルは命名規則で導出されます：`.opt-in.md` サフィックスを取り除き `.md` を追加する（例：`security-baseline.opt-in.md` → `security-baseline.md`）
3. この段階では完全なルールファイル（例：`security-baseline.md`）を読み込まない

**遅延ルール読み込み**：
- 要件分析 (Requirements Analysis) 中に、読み込まれた `*.opt-in.md` ファイルからのオプトインプロンプトをユーザーに提示する
- ユーザーが拡張機能にオプトインした場合、その時点で対応するルールファイル（命名規則で導出）を読み込む
- ユーザーがオプトアウトした場合、完全なルールファイルは一切読み込まれない（コンテキストを節約）
- 対応する `*.opt-in.md` ファイルが存在しない拡張機能は常時強制適用されます。ワークフロー開始時にそのルールファイルを即座に読み込んでください

**強制適用**（読み込み済み・有効化された拡張機能にのみ適用）：
- 拡張機能のルールはハード制約であり、任意のガイダンスではありません
- 各ステージで、モデルはそのステージの目的・生成される成果物・作業のコンテキストに基づいて、どの拡張機能ルールが適用可能かをインテリジェントに評価します。関連するルールのみを適用してください
- 現在のステージに適用不可のルールは、コンプライアンス・サマリーでN/Aとしてマークしてください（これはブロッキング所見ではありません）
- 有効化された拡張機能の適用可能なルールへの非準拠は**ブロッキング所見**です。解決するまでステージ完了を提示しないでください
- ステージ完了を提示する際は、拡張機能ルールのコンプライアンス・サマリーを含めてください（ルールごとにprepared/non-compliant/N/A、N/A判定の簡潔な理由付きで）

**条件付き強制適用**：拡張機能は条件付きで有効化・無効化される場合があります。オプトインメカニズムについては `inception/requirements-analysis.md` を参照してください。任意のステージで拡張機能を強制適用する前に、`aidlc-docs/aidlc-state.md` の `## Extension Configuration` セクションでその `Enabled` ステータスを確認してください。無効化された拡張機能はスキップし、audit.md にスキップを記録してください。設定が存在しない場合は強制適用をデフォルトとしてください。

## 必須 (MANDATORY)：コンテンツ検証
**重要 (CRITICAL)**：ファイルを作成する前に、`common/content-validation.md` のルールに従って必ずコンテンツを検証してください：
- Mermaid図の構文を検証する
- ASCIIアート図を検証する（`common/ascii-diagram-standards.md` 参照）
- 特殊文字を適切にエスケープする
- 複雑なビジュアルコンテンツにはテキスト代替を提供する
- コンテンツのパース互換性をテストする

## 必須 (MANDATORY)：質問ファイルのフォーマット
**重要 (CRITICAL)**：いずれかのフェーズで質問する際は、必ず質問フォーマットのガイドラインに従ってください。

**完全な質問フォーマットルール（以下を含む）については `common/question-format-guide.md` を参照してください**：
- 多肢選択形式（A、B、C、D、Eのオプション）
- `[Answer]:` タグの使用方法
- 回答の検証と曖昧さの解消

## 必須 (MANDATORY)：カスタムウェルカムメッセージ
**重要 (CRITICAL)**：ソフトウェア開発の依頼を開始する際は、必ずウェルカムメッセージを表示してください。

**ウェルカムメッセージの表示方法**：
1. `common/welcome-message.md`（解決されたルール詳細ディレクトリ内）からウェルカムメッセージを読み込む
2. 完全なメッセージをユーザーに表示する
3. これは新しいワークフロー開始時に一度だけ行う
4. コンテキスト容量を節約するため、以降のインタラクションではこのファイルを読み込まない

# アダプティブ・ソフトウェア開発ワークフロー

---

# インセプション・フェーズ (INCEPTION PHASE)

**目的**：計画、要件収集、アーキテクチャ上の意思決定

**焦点**：何を構築するか（WHAT）とその理由（WHY）を決定する

**インセプション・フェーズ (INCEPTION PHASE) のステージ**：
- ワークスペース検出 (Workspace Detection)（常時 (ALWAYS)）
- リバースエンジニアリング (Reverse Engineering)（条件付き (CONDITIONAL) - ブラウンフィールド (Brownfield) のみ）
- 要件分析 (Requirements Analysis)（常時 (ALWAYS) - 適応的深度）
- ユーザーストーリー (User Stories)（条件付き (CONDITIONAL)）
- ワークフロープランニング (Workflow Planning)（常時 (ALWAYS)）
- アプリケーション設計 (Application Design)（条件付き (CONDITIONAL)）
- ユニット生成 (Units Generation)（条件付き (CONDITIONAL)）

---

## ワークスペース検出 (Workspace Detection)（常時実行 (ALWAYS EXECUTE)）

1. **必須 (MANDATORY)**：初期ユーザーリクエストを完全な生入力とともに audit.md に記録する
2. `inception/workspace-detection.md` からすべてのステップを読み込む
3. ワークスペース検出を実行する：
   - 既存の aidlc-state.md を確認する（存在する場合は再開）
   - 既存コードのワークスペースをスキャンする
   - ブラウンフィールド (Brownfield) またはグリーンフィールド (Greenfield) を判定する
   - 既存のリバースエンジニアリング (Reverse Engineering) 成果物を確認する
4. 次のフェーズを決定する：リバースエンジニアリング (Reverse Engineering)（ブラウンフィールド (Brownfield) かつ成果物なしの場合）または要件分析 (Requirements Analysis)
5. **必須 (MANDATORY)**：調査結果を audit.md に記録する
6. 完了メッセージをユーザーに提示する（メッセージ形式は workspace-detection.md 参照）
7. 次のフェーズに自動的に進む

## リバースエンジニアリング (Reverse Engineering)（条件付き (CONDITIONAL) - ブラウンフィールド (Brownfield) のみ）

**実行条件**：
- 既存コードベースが検出された場合
- 過去のリバースエンジニアリング (Reverse Engineering) 成果物が存在しない場合

**スキップ条件**：
- グリーンフィールド (Greenfield) プロジェクト
- 過去のリバースエンジニアリング (Reverse Engineering) 成果物が存在する場合

**実行手順**：
1. **必須 (MANDATORY)**：リバースエンジニアリング (Reverse Engineering) 開始を audit.md に記録する
2. `inception/reverse-engineering.md` からすべてのステップを読み込む
3. リバースエンジニアリング (Reverse Engineering) を実行する：
   - すべてのパッケージとコンポーネントを分析する
   - ビジネストランザクションを網羅するシステム全体のビジネス概要を生成する
   - アーキテクチャドキュメントを生成する
   - コード構造ドキュメントを生成する
   - API ドキュメントを生成する
   - コンポーネントインベントリを生成する
   - ビジネストランザクションがコンポーネントをまたいでどのように実装されているかを示すインタラクション図を生成する
   - 技術スタックドキュメントを生成する
   - 依存関係ドキュメントを生成する

4. **明示的な承認を待つ**：詳細な完了メッセージを提示する（メッセージ形式は reverse-engineering.md 参照）- ユーザーが確認するまで進まない
5. **必須 (MANDATORY)**：ユーザーの応答を完全な生入力とともに audit.md に記録する

## 要件分析 (Requirements Analysis)（常時実行 (ALWAYS EXECUTE) - 適応的深度）

**常時実行**されますが、リクエストの明確さと複雑さに基づいて深度が変わります：
- **最小（Minimal）**：シンプルで明確なリクエスト - 意図分析のみを文書化
- **標準（Standard）**：通常の複雑さ - 機能要件と非機能要件を収集
- **包括的（Comprehensive）**：複雑でリスクの高い案件 - トレーサビリティを含む詳細な要件

**実行手順**：
1. **必須 (MANDATORY)**：このフェーズ中のユーザー入力を audit.md に記録する
2. `inception/requirements-analysis.md` からすべてのステップを読み込む
3. 要件分析 (Requirements Analysis) を実行する：
   - リバースエンジニアリング (Reverse Engineering) 成果物を読み込む（ブラウンフィールド (Brownfield) の場合）
   - ユーザーリクエストを分析する（意図分析）
   - 必要な要件の深度を決定する
   - 現在の要件を評価する
   - 明確化のための質問をする（必要な場合）
   - 要件ドキュメントを生成する
4. 適切な深度（minimal/standard/comprehensive）で実行する
5. **明示的な承認を待つ**：requirements-analysis.md の詳細ステップにある承認形式に従う - ユーザーが確認するまで進まない
6. **必須 (MANDATORY)**：ユーザーの応答を完全な生入力とともに audit.md に記録する

## ユーザーストーリー (User Stories)（条件付き (CONDITIONAL)）

**インテリジェント評価**：ユーザーストーリーが価値を追加するかどうかをマルチファクター分析で判断します：

**常時実行 (ALWAYS) 条件**（高優先度の指標）：
- ユーザー向けの新機能または機能追加
- ユーザーのワークフローまたはインタラクションに影響する変更
- 複数のユーザータイプまたはペルソナが関与する場合
- 受け入れ基準が必要な複雑なビジネス要件
- クロスファンクショナルなチームコラボレーションが必要な場合
- 顧客向け API またはサービスの変更
- 新製品の機能または拡張

**おそらく実行条件**（中優先度 - 複雑さを評価）：
- 既存のユーザー向け機能の変更
- 間接的にユーザー体験に影響するバックエンドの変更
- ユーザーワークフローに影響する統合作業
- ユーザーに見える改善を伴うパフォーマンス向上
- ユーザーインタラクションに影響するセキュリティ強化
- ユーザーデータやレポートに影響するデータモデルの変更

**複雑さに基づく評価**：中優先度のケースでは、以下の場合にユーザーストーリーを実行します：
- リクエストが複数のコンポーネントまたはサービスに関わる場合
- 変更が複数のユーザータッチポイントにまたがる場合
- ビジネスロジックが複雑または複数のシナリオを持つ場合
- 要件にストーリーが明確化できる曖昧さがある場合
- 実装が複数のユーザージャーニーに影響する場合
- 変更に重大なビジネスインパクトやリスクがある場合

**スキップのみ条件**（低優先度 - シンプルなケース）：
- ユーザーへの影響ゼロの純粋な内部リファクタリング
- スコープが明確で孤立したシンプルなバグ修正
- ユーザー向けの影響がないインフラ変更
- 機能変更なしの技術的負債の解消
- 開発者ツールまたはビルドプロセスの改善
- ドキュメントのみの更新

**評価基準**：判断に迷う場合、以下のケースではユーザーストーリーの追加を優先してください：
- ビジネスステークホルダーが関与するリクエスト
- ユーザー受け入れテストが必要な変更
- 複数の実装アプローチを持つ機能
- チームの共通理解から恩恵を受ける作業
- 要件の明確さが価値を持つプロジェクト

**評価プロセス**：
1. リクエストの複雑さとスコープを分析する
2. ユーザーへの影響を特定する（直接的または間接的）
3. ビジネスコンテキストとステークホルダーのニーズを評価する
4. チームコラボレーションの恩恵を考慮する
5. 境界線上のケースでは追加をデフォルトにする

**注意**：要件分析 (Requirements Analysis) が実行された場合、ストーリーはその要件を参照して構築することができます。

**ユーザーストーリー (User Stories) は一つのステージ内に2つのパートがあります**：
1. **パート1 - 計画**：質問を含むストーリー計画を作成し、回答を収集し、曖昧さを分析し、承認を得る
2. **パート2 - 生成**：承認された計画を実行してストーリーとペルソナを生成する

**実行手順**：
1. **必須 (MANDATORY)**：このフェーズ中のユーザー入力を audit.md に記録する
2. `inception/user-stories.md` からすべてのステップを読み込む
3. **必須 (MANDATORY)**：ユーザーストーリーが必要かを検証するためにインテリジェント評価（user-stories.md のステップ1）を実施する
4. リバースエンジニアリング (Reverse Engineering) 成果物を読み込む（ブラウンフィールド (Brownfield) の場合）
5. 要件が存在する場合、ストーリー作成時にそれらを参照する
6. 適切な深度（minimal/standard/comprehensive）で実行する
7. **パート1 - 計画**：質問を含むストーリー計画を作成し、ユーザーの回答を待ち、曖昧さを分析し、承認を得る
8. **パート2 - 生成**：承認された計画を実行してストーリーとペルソナを生成する
9. **明示的な承認を待つ**：user-stories.md の詳細ステップにある承認形式に従う - ユーザーが確認するまで進まない
10. **必須 (MANDATORY)**：ユーザーの応答を完全な生入力とともに audit.md に記録する

## ワークフロープランニング (Workflow Planning)（常時実行 (ALWAYS EXECUTE)）

1. **必須 (MANDATORY)**：このフェーズ中のユーザー入力を audit.md に記録する
2. `inception/workflow-planning.md` からすべてのステップを読み込む
3. **必須 (MANDATORY)**：`common/content-validation.md` からコンテンツ検証ルールを読み込む
4. すべての事前コンテキストを読み込む：
   - リバースエンジニアリング (Reverse Engineering) 成果物（ブラウンフィールド (Brownfield) の場合）
   - 意図分析
   - 要件（実行された場合）
   - ユーザーストーリー（実行された場合）
5. ワークフロープランニング (Workflow Planning) を実行する：
   - 実行するフェーズを決定する
   - 各フェーズの深度レベルを決定する
   - マルチパッケージ変更シーケンスを作成する（ブラウンフィールド (Brownfield) の場合）
   - ワークフローの可視化を生成する（書き込み前にMermaid構文を検証すること）
6. **必須 (MANDATORY)**：content-validation.md のルールに従ってファイル作成前にすべてのコンテンツを検証する
7. **明示的な承認を待つ**：workflow-planning.md のステップ9の言語を使って推奨事項を提示し、推奨事項を上書きするユーザーコントロールを強調する - ユーザーが確認するまで進まない
8. **必須 (MANDATORY)**：ユーザーの応答を完全な生入力とともに audit.md に記録する

## アプリケーション設計 (Application Design)（条件付き (CONDITIONAL)）

**実行条件**：
- 新しいコンポーネントまたはサービスが必要な場合
- コンポーネントのメソッドとビジネスルールの定義が必要な場合
- サービスレイヤーの設計が必要な場合
- コンポーネントの依存関係の明確化が必要な場合

**スキップ条件**：
- 既存のコンポーネント境界内での変更
- 新しいコンポーネントまたはメソッドが不要
- 純粋な実装変更

**実行手順**：
1. **必須 (MANDATORY)**：このフェーズ中のユーザー入力を audit.md に記録する
2. `inception/application-design.md` からすべてのステップを読み込む
3. リバースエンジニアリング (Reverse Engineering) 成果物を読み込む（ブラウンフィールド (Brownfield) の場合）
4. 適切な深度（minimal/standard/comprehensive）で実行する
5. **明示的な承認を待つ**：詳細な完了メッセージを提示する（メッセージ形式は application-design.md 参照）- ユーザーが確認するまで進まない
6. **必須 (MANDATORY)**：ユーザーの応答を完全な生入力とともに audit.md に記録する

## ユニット生成 (Units Generation)（条件付き (CONDITIONAL)）

**実行条件**：
- システムを複数のユニット・オブ・ワーク (unit of work) に分解する必要がある場合
- 複数のサービスまたはモジュールが必要な場合
- 構造化された分解が必要な複雑なシステム

**スキップ条件**：
- 単一のシンプルなユニット
- 分解が不要
- 単純な単一コンポーネントの実装

**実行手順**：
1. **必須 (MANDATORY)**：このフェーズ中のユーザー入力を audit.md に記録する
2. `inception/units-generation.md` からすべてのステップを読み込む
3. リバースエンジニアリング (Reverse Engineering) 成果物を読み込む（ブラウンフィールド (Brownfield) の場合）
4. 適切な深度（minimal/standard/comprehensive）で実行する
5. **明示的な承認を待つ**：詳細な完了メッセージを提示する（メッセージ形式は units-generation.md 参照）- ユーザーが確認するまで進まない
6. **必須 (MANDATORY)**：ユーザーの応答を完全な生入力とともに audit.md に記録する

---

# 🟢 コンストラクション・フェーズ (CONSTRUCTION PHASE)

**目的**：詳細設計、NFR実装、コード生成

**焦点**：どのように構築するか（HOW）を決定する

**コンストラクション・フェーズ (CONSTRUCTION PHASE) のステージ**：
- ユニットごとのループ（各ユニットに対して実行）：
  - 機能設計 (Functional Design)（条件付き (CONDITIONAL)、ユニットごと）
  - NFR要件 (NFR Requirements)（条件付き (CONDITIONAL)、ユニットごと）
  - NFR設計 (NFR Design)（条件付き (CONDITIONAL)、ユニットごと）
  - インフラ設計 (Infrastructure Design)（条件付き (CONDITIONAL)、ユニットごと）
  - コード生成 (Code Generation)（常時 (ALWAYS)、ユニットごと）
- ビルドとテスト (Build and Test)（常時 (ALWAYS) - すべてのユニット完了後）

**注意**：各ユニットは次のユニットに進む前に完全に（設計＋コード）完了させます。

---

## ユニットごとのループ（各ユニットに対して実行）

**各ユニット・オブ・ワーク (unit of work) に対して、以下のステージを順番に実行します：**

### 機能設計 (Functional Design)（条件付き (CONDITIONAL)、ユニットごと）

**実行条件**：
- 新しいデータモデルまたはスキーマがある場合
- 複雑なビジネスロジックがある場合
- ビジネスルールの詳細設計が必要な場合

**スキップ条件**：
- シンプルなロジック変更
- 新しいビジネスロジックなし

**実行手順**：
1. **必須 (MANDATORY)**：このステージ中のユーザー入力を audit.md に記録する
2. `construction/functional-design.md` からすべてのステップを読み込む
3. このユニットの機能設計 (Functional Design) を実行する
4. **必須 (MANDATORY)**：functional-design.md で定義された標準化された2オプション完了メッセージを提示する - 3オプションの創発的な動作を使用しない
5. **明示的な承認を待つ**：ユーザーは「変更を要求する」または「次のステージに進む」を選択する必要がある - ユーザーが確認するまで進まない
6. **必須 (MANDATORY)**：ユーザーの応答を完全な生入力とともに audit.md に記録する

### NFR要件 (NFR Requirements)（条件付き (CONDITIONAL)、ユニットごと）

**実行条件**：
- パフォーマンス要件がある場合
- セキュリティの考慮が必要な場合
- スケーラビリティの懸念がある場合
- 技術スタックの選定が必要な場合

**スキップ条件**：
- NFR要件なし
- 技術スタックがすでに決定済み

**実行手順**：
1. **必須 (MANDATORY)**：このステージ中のユーザー入力を audit.md に記録する
2. `construction/nfr-requirements.md` からすべてのステップを読み込む
3. このユニットのNFR評価を実行する
4. **必須 (MANDATORY)**：nfr-requirements.md で定義された標準化された2オプション完了メッセージを提示する - 創発的な動作を使用しない
5. **明示的な承認を待つ**：ユーザーは「変更を要求する」または「次のステージに進む」を選択する必要がある - ユーザーが確認するまで進まない
6. **必須 (MANDATORY)**：ユーザーの応答を完全な生入力とともに audit.md に記録する

### NFR設計 (NFR Design)（条件付き (CONDITIONAL)、ユニットごと）

**実行条件**：
- NFR要件 (NFR Requirements) が実行された場合
- NFRパターンを組み込む必要がある場合

**スキップ条件**：
- NFR要件なし
- NFR要件 (NFR Requirements) がスキップされた場合

**実行手順**：
1. **必須 (MANDATORY)**：このステージ中のユーザー入力を audit.md に記録する
2. `construction/nfr-design.md` からすべてのステップを読み込む
3. このユニットのNFR設計 (NFR Design) を実行する
4. **必須 (MANDATORY)**：nfr-design.md で定義された標準化された2オプション完了メッセージを提示する - 創発的な動作を使用しない
5. **明示的な承認を待つ**：ユーザーは「変更を要求する」または「次のステージに進む」を選択する必要がある - ユーザーが確認するまで進まない
6. **必須 (MANDATORY)**：ユーザーの応答を完全な生入力とともに audit.md に記録する

### インフラ設計 (Infrastructure Design)（条件付き (CONDITIONAL)、ユニットごと）

**実行条件**：
- インフラサービスのマッピングが必要な場合
- デプロイメントアーキテクチャが必要な場合
- クラウドリソースの仕様策定が必要な場合

**スキップ条件**：
- インフラ変更なし
- インフラがすでに定義済み

**実行手順**：
1. **必須 (MANDATORY)**：このステージ中のユーザー入力を audit.md に記録する
2. `construction/infrastructure-design.md` からすべてのステップを読み込む
3. このユニットのインフラ設計 (Infrastructure Design) を実行する
4. **必須 (MANDATORY)**：infrastructure-design.md で定義された標準化された2オプション完了メッセージを提示する - 創発的な動作を使用しない
5. **明示的な承認を待つ**：ユーザーは「変更を要求する」または「次のステージに進む」を選択する必要がある - ユーザーが確認するまで進まない
6. **必須 (MANDATORY)**：ユーザーの応答を完全な生入力とともに audit.md に記録する

### コード生成 (Code Generation)（常時実行 (ALWAYS EXECUTE)、ユニットごと）

**各ユニットに対して常時実行**

**コード生成 (Code Generation) は一つのステージ内に2つのパートがあります**：
1. **パート1 - 計画**：明示的なステップを含む詳細なコード生成計画を作成する
2. **パート2 - 生成**：承認された計画を実行してコード、テスト、成果物を生成する

**実行手順**：
1. **必須 (MANDATORY)**：このステージ中のユーザー入力を audit.md に記録する
2. `construction/code-generation.md` からすべてのステップを読み込む
3. **パート1 - 計画**：チェックボックス付きのコード生成計画を作成し、ユーザーの承認を得る
4. **パート2 - 生成**：承認された計画を実行してこのユニットのコードを生成する
5. **必須 (MANDATORY)**：code-generation.md で定義された標準化された2オプション完了メッセージを提示する - 創発的な動作を使用しない
6. **明示的な承認を待つ**：ユーザーは「変更を要求する」または「次のステージに進む」を選択する必要がある - ユーザーが確認するまで進まない
7. **必須 (MANDATORY)**：ユーザーの応答を完全な生入力とともに audit.md に記録する

---

## ビルドとテスト (Build and Test)（常時実行 (ALWAYS EXECUTE)）

1. **必須 (MANDATORY)**：このフェーズ中のユーザー入力を audit.md に記録する
2. `construction/build-and-test.md` からすべてのステップを読み込む
3. 包括的なビルドとテストの手順を生成する：
   - すべてのユニットのビルド手順
   - ユニットテスト実行手順
   - 統合テスト手順（ユニット間のインタラクションをテスト）
   - パフォーマンステスト手順（適用可能な場合）
   - 必要に応じた追加テスト手順（コントラクトテスト、セキュリティテスト、e2eテスト）
4. build-and-test/ サブディレクトリに手順ファイルを作成する：build-instructions.md、unit-test-instructions.md、integration-test-instructions.md、performance-test-instructions.md、build-and-test-summary.md
5. **明示的な承認を待つ**：「**ビルドとテストの手順が完了しました。オペレーションズ・ステージに進む準備はできていますか？**」と確認する - ユーザーが確認するまで進まない
6. **必須 (MANDATORY)**：ユーザーの応答を完全な生入力とともに audit.md に記録する

---

# 🟡 オペレーションズ・フェーズ (OPERATIONS PHASE)

**目的**：将来のデプロイメントおよびモニタリングワークフローのプレースホルダー (PLACEHOLDER)

**焦点**：どのようにデプロイして実行するか（将来の拡張）

**オペレーションズ・フェーズ (OPERATIONS PHASE) のステージ**：
- オペレーションズ (Operations)（プレースホルダー (PLACEHOLDER)）

---

## オペレーションズ (Operations)（プレースホルダー (PLACEHOLDER)）

**ステータス**：このステージは現在、将来の拡張のためのプレースホルダー (PLACEHOLDER) です。

オペレーションズ・ステージは最終的に以下を含む予定です：
- デプロイメントの計画と実行
- モニタリングとオブザーバビリティの設定
- インシデント対応手順
- メンテナンスとサポートのワークフロー
- プロダクション準備チェックリスト

**現状**：すべてのビルドとテストの活動はコンストラクション・フェーズ (CONSTRUCTION PHASE) で処理されます。

## 主要原則

- **適応的実行 (Adaptive Execution)**：価値を追加するステージのみを実行する
- **透明な計画 (Transparent Planning)**：開始前に常に実行計画を表示する
- **ユーザーコントロール (User Control)**：ユーザーはステージの追加・除外を要求できる
- **進捗追跡 (Progress Tracking)**：実行済みおよびスキップされたステージで aidlc-state.md を更新する
- **完全な監査証跡 (Complete Audit Trail)**：すべてのユーザー入力とAI応答をタイムスタンプ付きで audit.md に記録する
  - **重要 (CRITICAL)**：ユーザーの完全な生入力を正確に記録する
  - **重要 (CRITICAL)**：監査ログでユーザー入力を要約・言い換えない
  - **重要 (CRITICAL)**：承認のみでなくすべてのインタラクションを記録する
- **品質重視 (Quality Focus)**：複雑な変更は完全な処理を行い、シンプルな変更は効率的に処理する
- **コンテンツ検証 (Content Validation)**：content-validation.md のルールに従ってファイル作成前に常にコンテンツを検証する
- **創発的な動作禁止 (NO EMERGENT BEHAVIOR)**：コンストラクション・フェーズのステージは各ルールファイルで定義された標準化された2オプション完了メッセージを使用しなければなりません。3オプションメニューや他の創発的なナビゲーションパターンを作成しないでください。

## 必須 (MANDATORY)：計画レベルのチェックボックス強制

### 計画実行の必須ルール
1. **計画チェックボックスを更新せずに作業を完了することは禁止 (NEVER)**
2. **計画ファイルに記述されたいずれかのステップを完了した直後に、そのステップを [x] とマークする**
3. **これは作業が完了した同じインタラクション内で行わなければならない**
4. **例外なし**：すべての計画ステップの完了はチェックボックス更新で追跡しなければならない

### 2レベルチェックボックス追跡システム
- **計画レベル**：各ステージ内の詳細な実行進捗を追跡
- **ステージレベル**：aidlc-state.md でのワークフロー全体の進捗を追跡
- **即時更新**：すべての進捗更新は作業が完了した同じインタラクション内で行う

## プロンプトのロギング要件
- **必須 (MANDATORY)**：すべてのユーザー入力（プロンプト、質問、応答）をタイムスタンプ付きで audit.md に記録する
- **必須 (MANDATORY)**：ユーザーの完全な生入力を正確に記録する（要約しない）
- **必須 (MANDATORY)**：ユーザーに質問する前にタイムスタンプ付きですべての承認プロンプトを記録する
- **必須 (MANDATORY)**：受信後にタイムスタンプ付きですべてのユーザー応答を記録する
- **重要 (CRITICAL)**：変更を audit.md ファイルに常に追記・編集する。内容を完全に上書きするツールやコマンドは使用しない
- **重要 (CRITICAL)**：audit.md の内容全体を上書きするファイル書き込みツールやコマンドは使用しない（重複が発生する）
- タイムスタンプには ISO 8601 形式を使用する（YYYY-MM-DDTHH:MM:SSZ）
- 各エントリにステージコンテキストを含める

### 監査ログの形式：
```markdown
## [Stage Name or Interaction Type]
**Timestamp**: [ISO timestamp]
**User Input**: "[Complete raw user input - never summarized]"
**AI Response**: "[AI's response or action taken]"
**Context**: [Stage, action, or decision made]

---
```

### audit.md の正しいツール使用方法

✅ 正しい方法：

1. audit.md ファイルを Read で読み込む
2. ファイルを追記・編集して変更を加える

❌ 誤った方法：

1. audit.md ファイルを Read で読み込む
2. 読み込んだ内容と追加したい変更を合わせて audit.md を完全に上書きする

## ディレクトリ構造

```text
<WORKSPACE-ROOT>/                   # ⚠️ APPLICATION CODE HERE
├── [project-specific structure]    # Varies by project (see code-generation.md)
│
├── aidlc-docs/                     # 📄 DOCUMENTATION ONLY
│   ├── inception/                  # 🔵 INCEPTION PHASE
│   │   ├── plans/
│   │   ├── reverse-engineering/    # Brownfield only
│   │   ├── requirements/
│   │   ├── user-stories/
│   │   └── application-design/
│   ├── construction/               # 🟢 CONSTRUCTION PHASE
│   │   ├── plans/
│   │   ├── {unit-name}/
│   │   │   ├── functional-design/
│   │   │   ├── nfr-requirements/
│   │   │   ├── nfr-design/
│   │   │   ├── infrastructure-design/
│   │   │   └── code/               # Markdown summaries only
│   │   └── build-and-test/
│   ├── operations/                 # 🟡 OPERATIONS PHASE (placeholder)
│   ├── aidlc-state.md
│   └── audit.md
```

**重要 (CRITICAL) ルール**：
- アプリケーションコード：ワークスペースルート（aidlc-docs/ 内には禁止 (NEVER)）
- ドキュメント：aidlc-docs/ のみ
- プロジェクト構造：プロジェクトタイプ別のパターンについては code-generation.md を参照

---

# Claude Code ワークフロー

## ドキュメント構成

- **aidlc-docs/inception/project-overview.md** - プロジェクト概要（ビジョン・コンセプト・コアバリュー・ターゲットユーザー）。Ideation開始前に記入する。
- **aidlc-docs/ideation/** - Ideation フェーズの出力（customer_insights.md / ideas.md / commercialization.md）
- **docs/permanent/** - 永続的ドキュメント（要件定義・機能設計・技術仕様など）
- **.steering/[YYYYMMDD]-[開発タイトル]/** - 作業単位のステアリングファイル（requirements.md / design.md / tasklist.md）

## カスタムコマンド

| コマンド | 用途 |
|---------|------|
| `/init-project` | 新規プロジェクトの初回セットアップ（docs/ と .steering/ の作成） |
| `/new-feature` | 機能追加・修正時のステアリングディレクトリ作成ワークフロー |

## 開発ルール

### ドキュメント管理
- 永続的ドキュメント (docs/permanent/) は大きな設計変更時のみ更新
- 作業単位のドキュメント (.steering/) は作業ごとに新しいディレクトリを作成
- 図表・ダイアグラムは関連する永続的ドキュメント内に直接記載（Mermaid記法推奨）
